{
  "name": "cypress-structure",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:chrome": "cypress run --browser chrome --no-exit",
    "test:terminal": "cypress-tags run -e TAGS=@testedasa",
    "test:browser": "cypress-tags run -e TAGS=@testedasa --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.4.0",
    "cypress-cucumber-preprocessor": "^1.12.0"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/support/steps"
  }
}
